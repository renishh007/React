{
  "version": 2,
  "builds": [
    {
      "src": "Backend/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "Backend/index.js"
    }
  ],
  "buildCommand": "npm install --prefix Backend && npm install --prefix Client && npm run build --prefix Client"
}